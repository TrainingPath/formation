# Fichiers système
Thumbs.db
desktop.ini
.DS_Store

# Fichiers temporaires
*.tmp
*.bak
*~

# Éditeurs
.vscode/
.idea/

# ── Artefacts d'exécution générés par les EXERCICES et les VÉRIFICATEURS ──
# Le site ne contient aucun fichier de contenu .txt/.json/.csv/.dat/.log/.db :
# ces extensions sont donc toujours des sorties d'exécution (fil-rouge, solutions, _verify.js).
*.txt
*.json
*.csv
*.dat
*.log
*.db
*.sqlite
*.sqlite3
# Noms précis écrits par des solutions du site (redondant mais explicite)
taches.json
carnet.json
locations.txt
catalogue.txt
catalogue.json
classe.txt
data.json
jeu.txt
jeux.dat
journal.txt
notes.txt
scores.txt
f.txt

# ── Binaires / objets compilés ──
a.out
*.exe
*.o
*.class
*.jar
prog
prog.out

# ── Répertoire de travail dédié aux vérifications (ne JAMAIS commiter) ──
/_verify_tmp/

# ── Exceptions : contenu légitime du site portant une extension ignorée ──
# (le seul fichier « données/source » qui fait partie du site)
!cours-projet-asm/ludostat.s
